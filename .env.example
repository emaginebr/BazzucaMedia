# BazzucaMedia Docker Environment Configuration
# Copy this file to .env and update with your values

# ==========================================
# PostgreSQL Database Configuration
# ==========================================
POSTGRES_DB=bazzuca_db
POSTGRES_USER=bazzuca_user
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_PORT=5434

# ==========================================
# Connection String (for Docker containers)
# ==========================================
CONNECTION_STRING=Host=bazzuca-postgres;Port=5432;Database=bazzuca_db;Username=bazzuca_user;Password=your_secure_password_here

# ==========================================
# JWT Configuration
# ==========================================
# Generate a secure random string for production
# You can use: openssl rand -base64 64
JWT_SECRET=your_jwt_secret_key_min_32_chars_recommended_64_chars

# ==========================================
# BazzucaMedia API Configuration
# ==========================================
API_HTTP_PORT=5010
API_HTTPS_PORT=5011

# ==========================================
# BazzucaMedia App Configuration
# ==========================================
# Frontend application port
BAZZUCA_APP_PORT=5012

# Vite API URLs (used during build)
# These are embedded into the frontend during the Docker build process
VITE_API_URL=http://localhost:5004
VITE_BAZZUCA_API_URL=https://localhost:5011

# Node environment
# Options: development, production
NODE_ENV=development

# ==========================================
# ASP.NET Core Environment
# ==========================================
# Options: Development, Staging, Production, Docker
# - Development: Uses appsettings.Development.json, enables Swagger, detailed errors
# - Production: Uses appsettings.Production.json, minimal logging, optimized for production
# - Docker: Uses appsettings.Docker.json, container-specific settings
ASPNETCORE_ENVIRONMENT=Docker

# ==========================================
# SSL Certificate Configuration
# ==========================================
# IMPORTANTE: Gere o certificado antes de usar HTTPS
# Execute: .\generate-cert.ps1 (Windows) ou ./generate-cert.sh (Linux/Mac)
#
# Para desabilitar HTTPS:
# - Comente as linhas abaixo OU
# - Deixe CERTIFICATE_PATH e CERTIFICATE_PASSWORD vazios
# - A aplicação funcionará apenas em HTTP

CERTIFICATE_PATH=/app/certs/certificate.pfx
CERTIFICATE_PASSWORD=pikpro6
CERTIFICATE_DIRECTORY=./certs

# ==========================================
# Notes
# ==========================================
# - Change all passwords before deploying to production
# - Keep this file (.env) in .gitignore
# - Use strong, randomly generated passwords
# - JWT_SECRET should be at least 32 characters (64+ recommended)
# - For production, consider using environment variables or secrets management
# - API_HTTP_PORT: External HTTP port for BazzucaMedia API (default: 5010)
# - API_HTTPS_PORT: External HTTPS port for BazzucaMedia API (default: 5011)
# - CERTIFICATE_PATH: Path to SSL certificate inside container (/app/certs/certificate.pfx)
# - CERTIFICATE_DIRECTORY: Local directory containing SSL certificates (./certs)

# ==========================================
# Security Reminders
# ==========================================
# ? Never commit real passwords to version control
# ? Use valid certificates in production (not self-signed)
# ? Use secrets managers in production (Azure Key Vault, AWS Secrets Manager)
# ? Rotate passwords and certificates regularly
# ? This configuration is for development/testing only